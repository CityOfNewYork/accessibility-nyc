node_modules/
.DS_Store
*.log
.claude/
# Local working files — anything in internal/ stays out of the repo.
internal/
# Regenerable intermediates: the crawl manifest (input to check-links.mjs) and
# the JSON twin of dashboard/links-data.js (which is what the dashboard serves).
link-manifest.json
link-results.json

# One-off puppeteer probes written while working out how a specific site
# behaves. They are throwaway by design; anything worth keeping moves into
# scan-finders.mjs or scan-core.mjs.
drive-*.mjs
diag-*.mjs
